/**
 * Site copy defaults and types. Runtime text comes from the database via getSiteCopy()
 * (see @/lib/site-copy). Admin: /admin/site-copy
 */

export type { NavLinkDef, SiteCopy } from "./site-copy-defaults";
export { DEFAULT_SITE_COPY } from "./site-copy-defaults";
